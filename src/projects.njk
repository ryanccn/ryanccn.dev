---
layout: 'base'
title: 'Projects'
wide: true
---

<h1 class="title">Projects</h1>

<p class="subtitle mb-16">
  The open source projects that I have built myself and some that I've contributed to over the years.
</p>

<ul class="projects-grid" id="projects">
  {% asyncEach project in projects.projects %}
    <li>{% include "projectCard.njk" %}</li>
  {% endeach %}
</ul>

<h2 class="projects-page" id="minecraft">Organizations</h2>
<ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-2 mb-12">
  {% asyncEach org in organizations %}
    <li>
      <a href="{{ org.url }}" class="p-6 flex flex-col bg-surface hover:bg-surface-hover transition-all rounded h-full">
        <div class="flex flex-row gap-2 items-center mb-3">
          <div class="flex flex-col gap-y-0.5">
            <span class="text-xl font-bold leading-snug">{{ org.name }}</span>
            <span class="text-fg-less-dimmed font-medium leading-snug">{{ org.role }}</span>
          </div>
        </div>
        <p class="text-sm leading-snug text-fg-dimmed">{{ org.description }}</p>
      </a>
    </li>
  {% endeach %}
</ul>


<h2 class="projects-page" id="contributions">Other notable contributions</h2>
<ul class="projects-grid">
  {% asyncEach project in projects.contributions %}
    <li>{% include "projectCard.njk" %}</li>
  {% endeach %}
</ul>

<h2 class="projects-page" id="github">Contributed repositories on GitHub</h2>

<ol class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-x-4 gap-y-3 mb-12">
  {% asyncEach repo in contributions %}
  <li class="flex gap-x-2 justify-between w-full">
    <a rel="noreferrer" class="font-semibold text-sm overflow-hidden overflow-ellipsis whitespace-nowrap min-w-0 underline underline-offset-2 decoration-transparent hover:decoration-fg-less-dimmed transition-all" href="{{ repo.url }}">
      <span class="text-fg-less-dimmed">{{ repo.nameWithOwner.split('/')[0] }}/</span><span>{{ repo.nameWithOwner.split('/')[1] }}</span>
    </a>
    <div class="flex gap-x-1 items-center text-fg-dimmed">
      {% icon 'lucide:star', class='block w-3 h-3 fill-current stroke-none' %}
      <span class="text-xs font-medium">{{ repo.stargazers.totalCount | shortCount }}</span>
    </div>
  </li>
  {% endeach %}
</ol>

<h2 class="projects-page" id="minecraft">Minecraft Mods</h2>
<ul class="projects-grid">
  {% asyncEach project in projects.mods %}
    <li>{% include "projectCard.njk" %}</li>
  {% endeach %}
</ul>

<div class="opacity-50 hover:opacity-100 transition-opacity" id="archived">
  <h2 class="projects-page">Archived Projects</h2>
  <ul class="projects-grid">
    {% asyncEach project in projects.archived %}
      <li>{% include "projectCard.njk" %}</li>
    {% endeach %}
  </ul>
</div>
